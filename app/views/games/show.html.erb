<%= turbo_stream_from [@game, current_user] %>

<%= turbo_frame_tag "game_player_#{current_player.id}" do %>
  <%= render "games/actual_state", game: @game, current_player:, current_player_role: @game.player_role(current_player) %>
<% end %>
